# @package _global_
defaults:
  - override /dataset: last_fm
  - override /model: gcn

callbacks:
  model_checkpoint:
    filename: GCN-LastFM

dataset:
  homogeneous: true

tags:
  - GCN
  - LastFM
  - lp
  - gnn
  - exp2