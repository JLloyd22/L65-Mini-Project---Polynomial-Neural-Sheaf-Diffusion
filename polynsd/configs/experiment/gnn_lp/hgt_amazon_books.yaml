# @package _global_
defaults:
  - override /dataset: amazon_books
  - override /model: hgt

callbacks:
  model_checkpoint:
    filename: HGT-AmazonBooks

tags:
  - HGT
  - AmazonBooks
  - lp
  - gnn
  - exp2