# @package _global_
defaults:
  - override /dataset: movie_lens
  - override /model: rgcn

callbacks:
  model_checkpoint:
    filename: RGCN-MovieLens

tags:
  - RGCN
  - MovieLens
  - lp
  - gnn
  - exp2

dataset:
  homogeneous: true