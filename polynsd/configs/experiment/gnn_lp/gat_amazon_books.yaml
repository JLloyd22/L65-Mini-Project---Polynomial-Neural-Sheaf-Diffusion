# @package _global_
defaults:
  - override /dataset: amazon_books
  - override /model: gat

callbacks:
  model_checkpoint:
    filename: GAT-AmazonBooks

dataset:
  homogeneous: true

tags:
  - GAT
  - AmazonBooks
  - lp
  - gnn
  - exp2