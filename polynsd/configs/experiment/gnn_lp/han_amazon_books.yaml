# @package _global_
defaults:
  - override /dataset: amazon_books
  - override /model: han

callbacks:
  model_checkpoint:
    filename: HAN-AmazonBooks

tags:
  - HAN
  - AmazonBooks
  - lp
  - gnn
  - exp2

dataset:
  homogeneous: false