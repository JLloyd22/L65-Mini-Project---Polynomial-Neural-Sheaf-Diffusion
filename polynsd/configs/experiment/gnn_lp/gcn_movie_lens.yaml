# @package _global_
defaults:
  - override /dataset: movie_lens
  - override /model: gcn

callbacks:
  model_checkpoint:
    filename: GCN-MovieLens

dataset:
  homogeneous: true

tags:
  - GCN
  - MovieLens
  - lp
  - gnn
  - exp2