# @package _global_
defaults:
  - override /dataset: movie_lens
  - override /model: hgt

callbacks:
  model_checkpoint:
    filename: HGT-MovieLens

tags:
  - HGT
  - MovieLens
  - lp
  - gnn
  - exp2

dataset:
  homogeneous: false