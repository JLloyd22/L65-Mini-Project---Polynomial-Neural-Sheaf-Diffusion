# @package _global_
defaults:
  - override /dataset: amazon_books
  - override /model: gcn

callbacks:
  model_checkpoint:
    filename: GCN-AmazonBooks

dataset:
  homogeneous: true

tags:
  - GCN
  - AmazonBooks
  - lp
  - gnn
  - exp2